Create a new task: $ARGUMENTS

Follow the /add specification in CLAUDE.md. Parse the description for priority, due date, tags, and project. Generate a timestamp ID, create the task file in tasks/, and confirm what was created.