Process inbox items interactively.

Follow the /triage specification in CLAUDE.md. Walk through each inbox item, asking for project, priority, due date, and tags. Move processed items from inbox/ to tasks/ with full metadata. Accept short answers for speed.