Archive tasks: $ARGUMENTS

Follow the /archive specification in CLAUDE.md. With a task reference, archive that specific task. Without arguments, auto-archive all completed tasks older than the configured threshold.