Log time manually against a task: $ARGUMENTS

Follow the /clock specification in CLAUDE.md. Resolve the task reference, parse the duration, add a clock_log entry, update the clocked total, and append to logs/timesheet.csv.