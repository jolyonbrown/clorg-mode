# clorg-mode

Read `clorg-mode-spec.md` for the full project specification and build plan.

Start with Phase 1: Foundation (MVP). Build each piece incrementally 
and test as you go. Ask before moving to the next phase.

IMPORTANT: The first thing to build is the end-user CLAUDE.md system 
prompt — the one that goes in ~/clorg/CLAUDE.md and defines how 
clorg-mode behaves. Everything else is scaffolding around that file. 
Get that right first.
